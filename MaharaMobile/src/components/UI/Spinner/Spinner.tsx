import React from 'react';
import { View } from 'react-native';
import styles from './Spinner.style';

export const Spinner: React.FC = () => <View style={styles.view}></View>;

export default Spinner;
